组件是可以可以被重复使用的

@Service
    有了这个注解,将自动注册到spring容器中,不需要再在applicationContext里面定义bean了
    用于标注业务层组件

@Controller
    用于标注控制层组件
    
@Repository
    用于标注数据访问层组件
    
@Component
    泛指组件,当组件不好归类的时候,我们可以使用这个组件进行标注
    
@Autowired
    它可以对类成员变量,方法及构造函数进行标注,完成自动装配的工作.向容器中注入Bean
                
    
    